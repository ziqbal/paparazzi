

run.sh should return to starting directory not APP directory

have log file
- when script gets run by cron etc



camera.start_preview()
sleep(2)
camera.capture('/home/pi/selfie.jpg')
camera.stop_preview()



int ch;
    /* read character by character from stdin */
    do {
    ch = fgetc(stdin);
    putchar(ch);
    } while (ch != EOF);

   ch = fgetc(stdin);
                if(ch==32){
                        printf("zzz\n");
                }
    
